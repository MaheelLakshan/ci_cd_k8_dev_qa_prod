name: CI Pipeline

on:
  push:
    branches:
      - develop
    tags:
      - qa-v*
      - prod-v*

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Image
        run: |
          docker build -t maheellakshan/python-cicd:${{ github.sha }} ./app
          docker push maheellakshan/python-cicd:${{ github.sha }}
